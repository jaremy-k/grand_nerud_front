services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:5003}
        VITE_COOKIE_DOMAIN: ${VITE_COOKIE_DOMAIN:-.worldautogroup.ru}
    ports:
      - "${PORT:-3000}:80"
    restart: unless-stopped
